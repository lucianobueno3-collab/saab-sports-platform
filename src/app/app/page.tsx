import { InstalarView } from '@/components/pwa/instalar-view'

/** Atalho curto e fácil de ditar: /app leva ao mesmo passo a passo de /instalar. */
export default function AppPage() {
  return <InstalarView />
}
